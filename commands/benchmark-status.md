---
description: Mostra o estado do benchmark — config ativo, snapshots existentes e padrões acumulados
---

Leia `benchmark/config.md`, liste `benchmark/snapshots/` e leia `benchmark/historico.md`. Apresente em resumo curto: o que está sendo monitorado, quantas rodadas já aconteceram, quando foi a última, e quais padrões se repetiram. Se não houver config, diga para rodar `/benchmark-setup`.
